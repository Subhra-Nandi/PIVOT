# Phase 0 — schema layer. Later comments below mark where each phase's
# dependencies were added.
pydantic>=2.6,<3
pytest>=8,<9

pdfplumber>=0.11,<1
python-docx>=1.1,<2
fpdf2>=2.7,<3
openpyxl>=3.1,<4

httpx>=0.27,<1
beautifulsoup4>=4.12,<5
lxml>=5.0,<6
firecrawl-py>=1.0,<2

python-dotenv>=1.0,<2
google-genai>=1.0,<2
groq>=0.11,<1
openai>=1.50,<2

# Phase 8 — API layer (app/main.py). gunicorn is the process manager Azure
# App Service's Linux/Python runtime expects; uvicorn's worker class runs
# the actual ASGI app under it (see backend/startup.sh).
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
gunicorn>=22.0,<24
