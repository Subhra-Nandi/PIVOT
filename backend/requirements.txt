# Phase 0 — schema layer only. Later phases add fastapi/uvicorn (endpoint),
# pdfplumber/unstructured (ingestion), and the LLM provider SDKs (extraction).
pydantic>=2.6,<3
pytest>=8,<9

pdfplumber>=0.11,<1
python-docx>=1.1,<2
fpdf2>=2.7,<3